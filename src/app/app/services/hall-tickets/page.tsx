import React from 'react';
import { ServicePlaceholder } from '@/features/services/components/ServicePlaceholder';

export default function HallTicketsPage() {
  return <ServicePlaceholder title="Hall Tickets" description="Instant verified hall tickets are launching before the mid-semester exams." icon="??" />
}
